```java
int x = 5;
int y = 6;

!(x > 6) // true
(x == 6 && x == 5) // false
(x == 6 || x == 5) // true
(x > -1 && y < 10) // true

(x > 10 && x < 5) // Flawed because it's not possible for both to be true
```
